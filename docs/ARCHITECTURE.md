# Architecture

LUTSIA CopiClin is local-first: pywebview desktop shell, localhost FastAPI backend, React/Vite frontend, SQLite persistence, provider interfaces for transcription and LLM/codex-account/local models. No cloud backend.
