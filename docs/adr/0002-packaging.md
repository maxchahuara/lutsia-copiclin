# ADR 0002: Packaging

Decision: PyInstaller first for MVP. Nuitka may improve performance/binary properties later but adds complexity. Build per OS in GitHub Actions; do not assume cross-compilation.
